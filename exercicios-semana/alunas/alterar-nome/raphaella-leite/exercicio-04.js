   while ( true ) {
    let  getSenha  =  prompt ( "Insira a senha:" ) ;
    if ( getSenha  ==  2002 ) {
        alert ("Acesso Permitio") ;
        pausa ;
    } else {
        alert ( "Senha inválida" ) ;
    }
}